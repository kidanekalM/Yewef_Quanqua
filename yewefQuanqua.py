def letterToNumber(m):
    return string.ascii_lowercase.index(m)

def encrypt(plainText,key):
    # Find amharic character and if charachter is multiple of 1 find the multiple of 1 and add it
    
    return plainText
def decrypt(cipherText,key):
    return cipherText